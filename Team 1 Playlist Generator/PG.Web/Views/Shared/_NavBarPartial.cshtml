@*

*@
@{
}
<nav class="navbar navbar-expand-lg " id="myNavBar">
    @*<a class="navbar-brand" asp-controller="Home" asp-action="Index"><b>Playlist Generator</b></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>*@
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" asp-controller="Home" asp-action="Index">Home <span class="sr-only">(current)</span></a>
            </li>


            <li class="nav-item">
                <a class="nav-link" asp-controller="Playlists" asp-action="Index" id="mynewTag"><b>Playlists</b></a>
            </li>
            @if (User.Identity.IsAuthenticated)
            {
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Playlists" asp-action="MyPlaylists">My Playlists</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Playlists" asp-action="Create">Create Playlist</a>
                </li>
            }
            @if (User.IsInRole("Admin"))
            {

            }
            <partial name="_LoginPartial" />
        </ul>
    </div>
</nav>